name: dynamic_link_example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  share_plus: ^6.3.1
  mobile_scanner: ^3.2.0
  # url launchers
  url_launcher: ^6.1.10
  # uni link
  uni_links: ^0.5.1

  #Firebase
  firebase_core: ^2.3.0
  firebase_dynamic_links: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
